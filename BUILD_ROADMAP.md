# BUILD_ROADMAP.md — خطة بناء المنصة

> **المشروع:** منصة توصيل محلية — إسرائيل / كابول  
> **التاريخ:** 2026-05-07  
> **الهدف:** خطة العمل الكاملة من الصفر حتى الإنتاج  
> **الجمهور:** Tech Lead، Backend Engineer، Flutter Engineer، DevOps  
> **ملاحظة:** هذه الخطة تغطي الباكند كاملاً أولاً، ثم الفرونتند. لا يُبنى أي تطبيق جوال قبل اكتمال واختبار الـ API.

---

## نظرة عامة على المراحل

| المرحلة | الاسم | النوع | المخرج |
|---|---|---|---|
| 0 | التهيئة والإعداد | Backend | سيرفر يشتغل محلياً متصل بقاعدة بيانات |
| 1 | قاعدة البيانات | Backend | 37 جدول جاهز بالكامل |
| 2 | المصادقة (Auth) | Backend | نظام JWT + OTP يعمل لكل الأدوار |
| 3 | APIs الأساسية | Backend | CRUD كامل للمطاعم والمنتجات والمستخدمين |
| 4 | دورة حياة الأوردر | Backend | أوردر يمر بكل الحالات من البداية للتسليم |
| 5 | توزيع السائقين | Backend | Dispatch تلقائي مع Redis Locking |
| 6 | الـ Realtime | Backend | Socket.IO يوصل الأحداث فورياً لكل الأطراف |
| 7 | الإشعارات | Backend | FCM يرسل Push Notifications لكل التطبيقات |
| 8 | لوحة الأدمن | Backend | APIs كاملة لإدارة المنصة |
| 9 | تطبيقات الجوال | Frontend | Flutter — مطعم، سائق، زبون + Admin Web |
| 10 | النشر | DevOps | المنصة حية على الإنتاج |

---

## المرحلة 0 — التهيئة والإعداد

**الهدف:** بيئة تطوير كاملة تعمل محلياً قبل كتابة أي كود وظيفي.

| # | المهمة | التفاصيل |
|---|---|---|
| 0.1 | تثبيت الأدوات | Node.js 20 LTS، Git، Docker Desktop، VSCode |
| 0.2 | إنشاء مشروع NestJS | `nest new delivery-backend` + إعداد TypeScript الصارم |
| 0.3 | إعداد `docker-compose.yml` | PostgreSQL 15 + Redis 7 يشتغلان بأمر واحد |
| 0.4 | ربط Prisma | تثبيت Prisma، إعداد `DATABASE_URL`، أول `prisma generate` |
| 0.5 | إعداد ملف `.env` | كل المتغيرات من `EXTERNAL_SERVICES.md` — SMS على `mock` |
| 0.6 | رفع المشروع على GitHub | `.gitignore` يشمل `.env` — لا أسرار في الـ Repository |
| 0.7 | التحقق النهائي | السيرفر يرد على `GET /health` بـ 200 ✓ |

**المخرج:** `localhost:3000` يشتغل ومتصل بـ PostgreSQL وRedis.

---

## المرحلة 1 — قاعدة البيانات

**الهدف:** ترجمة `DATABASE_SCHEMA.md` كاملاً إلى Prisma Schema وتشغيل الـ Migrations.

| # | المهمة | التفاصيل |
|---|---|---|
| 1.1 | كتابة `schema.prisma` | كل الجداول والعلاقات والـ Enums من `DATABASE_SCHEMA.md` |
| 1.2 | تعريف الـ Enums | `UserRole`، `OrderStatus`، `DriverStatus`، `PaymentMethod`... |
| 1.3 | تعريف العلاقات | Foreign Keys، One-to-Many، Many-to-Many بشكل صحيح |
| 1.4 | إضافة الـ Indexes | على الحقول الأكثر استخداماً في الاستعلامات |
| 1.5 | تشغيل أول Migration | `prisma migrate dev --name init` |
| 1.6 | التحقق من الجداول | مراجعة الجداول في قاعدة البيانات والتأكد من التطابق مع `ERD.md` |

**المخرج:** 37 جدول جاهز في PostgreSQL مع كل القيود والعلاقات.

---

## المرحلة 2 — المصادقة (Auth)

**الهدف:** نظام تسجيل دخول كامل يعمل لكل الأدوار — زبون، سائق، مطعم، أدمن.

| # | المهمة | التفاصيل |
|---|---|---|
| 2.1 | بناء `AuthModule` | Controller، Service، DTOs |
| 2.2 | OTP عبر SMS Mock | يطبع الكود في الـ Console — لا رسائل حقيقية في v0.1 |
| 2.3 | إصدار Access Token | JWT قصير الأجل (15 دقيقة) — RS256 |
| 2.4 | إصدار Refresh Token | JWT طويل الأجل (30 يوم) — يُخزن في قاعدة البيانات |
| 2.5 | تجديد التوكن | `POST /auth/refresh` — يُلغي القديم ويُصدر جديداً |
| 2.6 | تسجيل الخروج | يُلغي الـ Refresh Token من قاعدة البيانات |
| 2.7 | JWT Guard | يحمي كل الـ Endpoints ويتحقق من التوكن |
| 2.8 | Roles Guard | `@Roles('CUSTOMER', 'DRIVER'...)` على كل Endpoint |
| 2.9 | Rate Limiting | 5 محاولات OTP في الدقيقة، 100 طلب عام في الدقيقة |
| 2.10 | اختبار كامل | تسجيل ← دخول ← توكن ← تجديد ← خروج — عبر Postman |

**المخرج:** نظام مصادقة آمن يعمل لكل الأدوار السبعة.

---

## المرحلة 3 — APIs الأساسية

**الهدف:** كل عمليات CRUD للكيانات الرئيسية في المنصة.

| # | المهمة | التفاصيل |
|---|---|---|
| 3.1 | `RestaurantsModule` | إنشاء مطعم، تعديله، قائمة المطاعم، تفاصيل مطعم، تغيير الحالة (Open/Closed/Busy) |
| 3.2 | `MenuModule` | إضافة منتج، تعديله، حذفه، تفعيله وتعطيله، إدارة الفئات |
| 3.3 | `CustomersModule` | ملف الزبون، العناوين المحفوظة (إضافة، تعديل، حذف، تعيين الافتراضي) |
| 3.4 | `DriversModule` | ملف السائق، رفع بيانات التحقق، تبديل Online/Offline |
| 3.5 | `UsersModule` | إدارة عامة للمستخدمين (تعليق، تفعيل، استعلام) |
| 3.6 | اختبار كامل | كل Endpoint مختبر بـ Postman مع كل الأدوار |

**المخرج:** كل الـ APIs الأساسية جاهزة وموثقة ومختبرة.

---

## المرحلة 4 — دورة حياة الأوردر

**الهدف:** أوردر يُنشأ ويمر بكل الحالات بشكل صحيح مع حماية من التكرار.

| # | المهمة | التفاصيل |
|---|---|---|
| 4.1 | `OrdersModule` | إنشاء أوردر مع التحقق من المطعم والمنتجات والعنوان |
| 4.2 | Idempotency Key | UUID يُرسله التطبيق — يمنع تكرار الأوردر عند إعادة المحاولة |
| 4.3 | State Machine | كل الانتقالات بين الحالات مع التحقق من الدور والحالة الحالية |
| 4.4 | منطق الرفض التلقائي | المطعم لا يرد خلال وقت محدد → رفض تلقائي + إشعار للزبون |
| 4.5 | إلغاء الأوردر | الزبون يلغي في `PENDING_RESTAURANT` فقط — الأدمن يلغي في أي وقت |
| 4.6 | `OrderItems` | تخزين نسخة من اسم المنتج وسعره وقت الطلب (Snapshot) |
| 4.7 | `PaymentsModule` | تسجيل معاملة الدفع — نقدي فقط في MVP |
| 4.8 | اختبار كامل | أوردر كامل من الإنشاء حتى `DELIVERED` عبر Postman |

**المخرج:** أوردر يمر بـ 13 حالة بشكل صحيح مع حماية من التكرار والتلاعب.

---

## المرحلة 5 — توزيع السائقين (Dispatch)

**الهدف:** النظام يجد السائق المناسب تلقائياً ويُعيّنه بأمان.

| # | المهمة | التفاصيل |
|---|---|---|
| 5.1 | `DispatchModule` | خوارزمية البحث — استعلام السائقين Online في نطاق معين |
| 5.2 | حساب المسافة | Haversine Formula في الباكند — لا Google Maps API |
| 5.3 | ترتيب السائقين | الأقرب أولاً — مع إمكانية الترجيح بالتقييم مستقبلاً |
| 5.4 | Redis Atomic Lock | `SET order:{id}:lock NX EX 30` — يمنع تعيين سائقين لنفس الأوردر |
| 5.5 | نظام العرض | عرض الأوردر على السائق عبر Socket.IO + FCM كـ Fallback |
| 5.6 | Timeout (30 ثانية) | السائق لم يرد → يُعرض على التالي تلقائياً |
| 5.7 | توسيع النطاق | لا يوجد سائق في 5كم → يوسع لـ 7كم → 10كم |
| 5.8 | حالة FAILED | لا سائق بعد كل المحاولات → إشعار للأدمن والزبون |
| 5.9 | تعيين يدوي | الأدمن يعيّن سائقاً يدوياً من اللوحة — يُسجل في Audit Log |
| 5.10 | اختبار كامل | محاكاة منافسة سائقين على نفس الأوردر — يُعيَّن واحد فقط |

**المخرج:** توزيع تلقائي آمن بدون race conditions أو تعيين مزدوج.

---

## المرحلة 6 — الـ Realtime (Socket.IO)

**الهدف:** كل تغيير في الأوردر يصل فورياً لكل الأطراف المعنية.

| # | المهمة | التفاصيل |
|---|---|---|
| 6.1 | `RealtimeModule` | بوابة Socket.IO داخل NestJS مع JWT Authentication |
| 6.2 | نظام الـ Rooms | كل أوردر له غرفة مستقلة `order:{orderId}` |
| 6.3 | انضمام للغرف | الزبون والمطعم والسائق ينضمون للغرفة المناسبة عند الاتصال |
| 6.4 | إرسال أحداث الأوردر | كل تغيير في الحالة يُبث للغرفة المعنية فوراً |
| 6.5 | تتبع موقع السائق | السائق يرسل موقعه → يُخزن في Redis → يُبث للزبون والمطعم |
| 6.6 | Redis Adapter | `@socket.io/redis-adapter` لدعم تشغيل أكثر من Instance مستقبلاً |
| 6.7 | مزامنة عند الاتصال | عند Reconnect → يُرسل آخر حالة للأوردر مباشرة |
| 6.8 | اختبار كامل | محاكاة اتصال 3 أطراف ومتابعة تدفق الأحداث |

**المخرج:** كل حدث في الأوردر يصل للمطعم والزبون والسائق في أقل من ثانية.

---

## المرحلة 7 — الإشعارات (FCM)

**الهدف:** الإشعارات تصل حتى لو التطبيق مغلق أو الإنترنت ضعيف.

| # | المهمة | التفاصيل |
|---|---|---|
| 7.1 | `NotificationsModule` | ربط Firebase Admin SDK بـ NestJS |
| 7.2 | تخزين FCM Tokens | كل جهاز يسجّل Token عند الدخول — يُحدَّث عند كل تسجيل |
| 7.3 | تنظيف Tokens المنتهية | عند فشل الإرسال بـ `NOT_REGISTERED` → حذف تلقائي |
| 7.4 | إرسال إشعار لكل حدث | 11 نوع إشعار موثق في `DELIVERY_APPS_REQUIREMENTS.md` |
| 7.5 | أولوية HIGH | للأحداث الحرجة (أوردر جديد للمطعم، طلب توصيل للسائق) |
| 7.6 | Fallback Strategy | Socket.IO منقطع → FCM يُرسل مباشرة بدون انتظار |
| 7.7 | سجل الإشعارات | كل إشعار يُحفظ في جدول `notifications` للعرض في التطبيق |
| 7.8 | اختبار كامل | إشعار يصل لجهاز حقيقي مع تفعيل FCM credentials |

**المخرج:** كل إشعار يصل لكل طرف في الوقت الصحيح بغض النظر عن حالة الاتصال.

---

## المرحلة 8 — لوحة الأدمن (Backend APIs)

**الهدف:** الأدمن يقدر يتحكم بكل شيء في المنصة عبر APIs محمية.

| # | المهمة | التفاصيل |
|---|---|---|
| 8.1 | `AdminModule` | Controller محمي بـ `@Roles('ADMIN', 'SUPER_ADMIN')` |
| 8.2 | إدارة المستخدمين | عرض، بحث، تعليق، رفع حظر أي مستخدم |
| 8.3 | إدارة المطاعم | موافقة، تعليق، تعديل، عرض تاريخ الأوردرات |
| 8.4 | إدارة السائقين | قبول أو رفض طلبات التحقق، عرض مواقعهم |
| 8.5 | إدارة الأوردرات | إلغاء، تغيير حالة، تعيين سائق يدوي |
| 8.6 | إعدادات المنصة | نسبة العمولة، وقت الـ Timeout، الحد الأدنى للطلب |
| 8.7 | `AuditLog` | كل عملية أدمن تُسجل — من فعل ماذا ومتى |
| 8.8 | اختبار كامل | كل Endpoint مختبر بأدوار مختلفة عبر Postman |

**المخرج:** الأدمن يقدر يدير المنصة كاملاً عبر APIs جاهزة للـ Dashboard.

---

## المرحلة 9 — تطبيقات الجوال والويب (Frontend)

> **ملاحظة مهمة:** هذه المرحلة تبدأ فقط بعد اكتمال واختبار المراحل 0–8 كاملاً.

### 9.1 إعداد Monorepo

| # | المهمة | التفاصيل |
|---|---|---|
| 9.1.1 | إعداد Melos Workspace | تهيئة Monorepo يضم التطبيقات الثلاثة والحزم المشتركة |
| 9.1.2 | `shared_models` | Dart classes لكل كيان (Order، Restaurant، Driver...) |
| 9.1.3 | `shared_services` | Dio HTTP client، Socket.IO wrapper، FCM setup، Secure Storage |
| 9.1.4 | `shared_ui` | AppButton، AppCard، SkeletonLoader، MapWidget، EmptyState |
| 9.1.5 | `shared_theme` | ألوان، خطوط، مسافات — Light/Dark theme |
| 9.1.6 | `shared_utils` | formatCurrency (ILS/₪)، formatDate، generateIdempotencyKey |

### 9.2 تطبيق المطعم — الأول لأنه الأبسط

| # | المهمة |
|---|---|
| 9.2.1 | تسجيل الدخول + استقبال توكن |
| 9.2.2 | شاشة الأوردرات الحية |
| 9.2.3 | قبول أو رفض أوردر + تحديد سبب الرفض |
| 9.2.4 | تحديث حالة الأوردر (Preparing, Ready) |
| 9.2.5 | طلب سائق ومتابعة حالة التعيين |
| 9.2.6 | تبديل حالة المطعم (Open / Closed / Busy) |
| 9.2.7 | تفعيل وتعطيل المنتجات |
| 9.2.8 | استقبال الإشعارات عبر FCM |

### 9.3 تطبيق السائق — الثاني

| # | المهمة |
|---|---|
| 9.3.1 | تسجيل الدخول + رفع بيانات التحقق |
| 9.3.2 | شاشة انتظار الطلبات (Online/Offline) |
| 9.3.3 | استقبال طلب توصيل مع Countdown 30 ثانية |
| 9.3.4 | قبول أو رفض الطلب |
| 9.3.5 | تدفق التوصيل الكامل — ناحية المطعم ← وصل ← تسلّم ← ناحية الزبون ← سُلّم |
| 9.3.6 | إرسال الموقع الحي عبر Socket.IO |
| 9.3.7 | "فتح في خرائط" للتنقل (Google Maps / Waze deep link) |
| 9.3.8 | شاشة الأرباح اليومية |

### 9.4 تطبيق الزبون — الثالث

| # | المهمة |
|---|---|
| 9.4.1 | تسجيل دخول بـ +972 OTP |
| 9.4.2 | قائمة المطاعم مع الفلاتر |
| 9.4.3 | صفحة المطعم والقائمة |
| 9.4.4 | السلة (Persistent Cart) |
| 9.4.5 | إتمام الطلب — نقداً عند التسليم |
| 9.4.6 | تتبع الأوردر (Status Timeline) |
| 9.4.7 | خريطة السائق الحية بعد الاستلام |
| 9.4.8 | سجل الطلبات السابقة |
| 9.4.9 | الإشعارات عبر FCM |

### 9.5 لوحة الأدمن (React / Next.js) — الأخيرة

| # | المهمة |
|---|---|
| 9.5.1 | تسجيل الدخول |
| 9.5.2 | قائمة الأوردرات مع الفلاتر |
| 9.5.3 | قائمة المستخدمين والمطاعم والسائقين |
| 9.5.4 | قبول أو رفض تحقق السائقين |
| 9.5.5 | إلغاء أوردر يدوياً |
| 9.5.6 | الخريطة الحية (مواقع السائقين والأوردرات) |

---

## المرحلة 10 — النشر على الإنتاج

**الهدف:** المنصة تعمل على سيرفر حقيقي ومتاحة للمستخدمين.

| # | المهمة | التفاصيل |
|---|---|---|
| 10.1 | إعداد السيرفر | Hetzner CX21 أو DigitalOcean — Ubuntu 22.04 |
| 10.2 | تثبيت Node.js + PM2 + Nginx | PM2 لإدارة العملية، Nginx كـ Reverse Proxy |
| 10.3 | إعداد PostgreSQL المُدار | Neon أو Railway أو DigitalOcean Managed DB |
| 10.4 | إعداد Redis المُدار | Upstash أو DigitalOcean Managed Redis |
| 10.5 | إعداد SSL | Certbot + Let's Encrypt أو Cloudflare Proxy |
| 10.6 | ربط الدومين | DNS records → Cloudflare → السيرفر |
| 10.7 | FCM الحقيقي | رفع Firebase credentials الحقيقية |
| 10.8 | SMS الحقيقي (+972) | ربط Inforu أو 019 Mobile — اختبار وصول رسالة حقيقية |
| 10.9 | إعداد `CORS_ORIGINS` | يشمل عناوين التطبيقات الحقيقية فقط |
| 10.10 | تشغيل Migrations على الإنتاج | `prisma migrate deploy` |
| 10.11 | اختبار الـ Critical Path | أوردر كامل من الزبون حتى التسليم على الإنتاج الحقيقي |
| 10.12 | UptimeRobot | مراقبة `GET /health` كل 5 دقائق |
| 10.13 | Sentry | ربط تتبع الأخطاء |

**المخرج:** المنصة حية، آمنة، تحت المراقبة، جاهزة للاستخدام الفعلي.

---

## الترتيب المنطقي الكامل

```
المرحلة 0 — الإعداد
    ↓
المرحلة 1 — قاعدة البيانات
    ↓
المرحلة 2 — Auth
    ↓
المرحلة 3 — APIs الأساسية
    ↓
المرحلة 4 — دورة حياة الأوردر
    ↓
المرحلة 5 — Dispatch (توزيع السائقين)
    ↓
المرحلتان 6 و 7 معاً — Realtime + FCM
    ↓
المرحلة 8 — Admin APIs
    ↓
المرحلة 9 — Flutter + Admin Web (بالترتيب: مطعم ← سائق ← زبون ← أدمن)
    ↓
المرحلة 10 — النشر على الإنتاج
```

---

## قواعد العمل

| القاعدة | التفاصيل |
|---|---|
| الباكند أولاً | لا يُبنى أي تطبيق Flutter قبل اكتمال واختبار الـ API |
| Postman لكل Endpoint | كل API يُختبر يدوياً قبل الانتقال للتالي |
| لا تُضاف ميزة خارج الـ Scope | أي إضافة تُراجع مع `MVP_TASKS.md` أولاً |
| `.env` لا يُرفع على GitHub | كل الأسرار في متغيرات البيئة فقط |
| Migration قبل أي تغيير في الـ Schema | لا تعديل مباشر على قاعدة البيانات |
| كل Module يُكتمل قبل التالي | لا نبدأ مرحلة جديدة بدون اختبار السابقة |

---

## ملاحظات للمهندس

- **المصدر الرئيسي:** كل القرارات التقنية موثقة في الملفات الموجودة (`BACKEND_STRUCTURE.md`، `API_CONTRACTS.md`، `DATABASE_SCHEMA.md`...)
- **العملة:** كل المبالغ المالية بالـ ILS / ₪ — لا USD في البيانات
- **الهاتف:** كل أرقام الهواتف بصيغة `+972XXXXXXXXX`
- **الخرائط:** flutter_map + OpenStreetMap مجاناً في MVP — لا Google Maps API
- **الدفع:** نقداً فقط في MVP — لا بوابة دفع الآن
- **SMS:** Mock في التطوير المحلي — Inforu أو 019 Mobile في الإنتاج

---

*يُرجى مراجعة هذا الملف والتعليق على أي مرحلة تحتاج تعديلاً أو توضيحاً قبل البدء.*
